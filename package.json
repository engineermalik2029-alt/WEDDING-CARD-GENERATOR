{
  "name": "premium-wedding-invitation-studio",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered premium wedding invitation SPA with React, Express, OpenAI, MongoDB, and Cloudinary.",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}